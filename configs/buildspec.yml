version: 0.2
phases:
  install:
    commands:
      - apt-get update && apt-get install -y libsasl2-dev
      - pip install -r requirements.txt  # Install dependencies
  pre_build:
    commands:
      - echo Running linting checks
      - bash scripts/linting-checks.sh  # Run linting checks (flake8, pylint)
  build:
    commands:
artifacts:
  files:
    - dags/*
